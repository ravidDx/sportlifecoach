import { EntrenamientosPipe } from './entrenamientos.pipe';

describe('EntrenamientosPipe', () => {
  it('create an instance', () => {
    const pipe = new EntrenamientosPipe();
    expect(pipe).toBeTruthy();
  });
});
